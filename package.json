{
  "name": "micro-saas-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix micro-saas-app\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^7.4.0"
  }
}
